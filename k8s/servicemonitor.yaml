apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: website
  labels:
    app: website
    release: monitoring
spec:
  selector:
    matchLabels:
      app: website
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
