apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: payment
  labels:
    app: payment
    release: monitoring
spec:
  selector:
    matchLabels:
      app: payment
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
