apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - postgres.yaml

configMapGenerator:
  - name: postgres-initdb
    files:
      - init.sql=init.sql

images:
  - name: website
    newTag: local
